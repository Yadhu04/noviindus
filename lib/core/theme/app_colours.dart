import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color backgroundPrimary = Color(0xFF171717);
  static const Color backgroundSecondary = Color(0xFF1A1A1A);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFD5D5D5);

  // Button
  static const Color buttonPrimary = Color(0xFFC70000);
}
